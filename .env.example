VITE_BASE_URL=http://localhost:3000

# Database connection string for PostgreSQL
# For local development with Docker Compose: postgresql://username:password@localhost:5432/dbname
DATABASE_URL="postgresql://username:password@localhost:5432/tanstack-ecom"

# Docker Compose database credentials (update these)
POSTGRES_USER=username
POSTGRES_PASSWORD=password
POSTGRES_DB=tanstack-ecom
# You can also use Docker Compose to set up a local PostgreSQL database:
# docker-compose up -d
# pnpm run auth:secret
BETTER_AUTH_SECRET=
# OAuth2 providers, optional, update as needed
# GitHub: https://github.com/settings/developers
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Google: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Mercado Pago credentials
# Get from: https://www.mercadopago.com.ar/developers/panel/app
MERCADO_PAGO_ACCESS_TOKEN=
MERCADO_PAGO_PUBLIC_KEY=
# Webhook secret for signature verification (optional but recommended)
MERCADO_PAGO_WEBHOOK_SECRET=

# NOTE:
# In your OAuth2 apps, set callback/redirect URIs to:
# http://localhost:3000/api/auth/callback/<provider>
# e.g. http://localhost:3000/api/auth/callback/github
# e.g. http://localhost:3000/api/auth/callback/google
# 
# In Mercado Pago, set webhook URL to:
# https://yourdomain.com/api/webhooks/mercadopago